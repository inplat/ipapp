Компонент для распределенных блокирвок
======================================

Иногда нужно, чтоб задачи, выполняющие операции над одним ресурсом шли строго последовательно, либо нужно исключить состояние гонки.

.. literalinclude:: ../../examples/lock.py
   :caption: examples/helloworld.py
   :language: python
   :linenos:

В данном примере запросы идущие на один и тот же url будут выполняться строго последовательно, вне зависимости от количества запущенных экземпляров приложения
