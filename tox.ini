[tox]
envlist = py37,py38

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
commands =
    pip install --index-url https://pip.sys.ipl/simple --trusted-host pip.sys.ipl -r {toxinidir}/requirements_dev.txt
    pytest -s -v tests